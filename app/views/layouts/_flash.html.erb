<% flash.each do |key, msg| %>
  <% next if msg.blank? %>
  <%
    styles = case key.to_s
    when "notice" then "bg-green-500/10 border-green-500/30 text-green-400 shadow-green-500/5"
    when "alert"  then "bg-red-500/10 border-red-500/30 text-red-400 shadow-red-500/5"
    else "bg-orange-500/10 border-orange-500/30 text-orange-400 shadow-orange-500/5"
    end
  %>
  <div class="fixed top-14 sm:top-16 right-4 z-[110] w-auto max-w-sm animate-flash pointer-events-none">
    <div class="pointer-events-auto <%= styles %> px-4 py-2.5 rounded-xl text-xs sm:text-sm font-bold shadow-xl backdrop-blur-xl break-words leading-snug">
      <%= msg %>
    </div>
  </div>
<% end %>
